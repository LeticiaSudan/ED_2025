//Alocação Estática de Memória = definida em tempo de COMPILAÇÃO
//Alocalçao Dinâmica de Memória = definida em tempo de EXECUÇÃO

#include <stdio.h>
#include <stdlib.h>

int main(){
    //int v[50000000]; Não cabe na memória de alocação estática

    // int *v = (int *) malloc (1000000 * sizeof(int)); //a variável *v recebe o endereço de memória do vetor
    printf("foi");
    

/* 
    int  a = 8;
    int * b = &a;
    int **c = &b;
    */

   return 0;

    

}